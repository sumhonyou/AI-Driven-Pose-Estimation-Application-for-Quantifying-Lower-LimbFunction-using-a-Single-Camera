"""Single-Leg Stance (SLS) rebuild: both legs, lift-line gate, ball-in-circle stability.

Pure, deterministic analysis over a posted world-landmark sequence — no HTTP
dependency, so `analysis.analyze_leg()` is callable directly by the replay harness.
"""
