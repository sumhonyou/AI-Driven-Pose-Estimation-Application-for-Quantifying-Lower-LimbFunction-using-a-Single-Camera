# Environment
.env
.env.local

# Logs
development-log.md
Session_Summary_Log.md

# Local research / working docs (not for version control)
# docs/*
# !docs/module_b_decisions.md

# Local agent instructions (not for version control)
/AGENTS.md
/rules.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# ml/ — landmarks cache + feature CSVs are regenerable from the dataset, not committed
# (ml/artifacts/ and ml/reports/ ARE committed — see task.md Stage 5.1)
ml/data/*
!ml/data/.gitkeep

# Node
node_modules/
dist/
*.local

# IDE / OS
.idea/
.vscode/
.DS_Store
